import 'server-only';

// TODO(Phase 1): populate vision_pass_prompt + pet_profile_schema (JSON Schema).

export const VISION_PASS_PROMPT = '' as const;
export const PET_PROFILE_SCHEMA = {} as const;
